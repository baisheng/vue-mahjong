{lib_dirs, ["../../deps","../../application"]}.

%%{erl_opts, [no_debug_info]}.

{xref_checks, [undefined_function_calls]}.

{pre_hooks, [{compile, "make all"}]}.

{post_hooks, [{clean, "make clean"}]}.

